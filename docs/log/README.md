# Лог проекта

Хроника важных событий Плазмобота: железо, софт, прошивка, документация.
Пополняется автоматически раз в день в 12:00.

<!-- LOG:INSERT -->

## 2026-09-24

- ProtoChat v2.5.1 выкачена (00:40): разъёмы компонента не все доезжали до схемы проекта — снимок брал только `exposed`-порты плюс те, чей id совпал с заглушкой новой карточки (из десяти приходили четыре наугад); параллельно редактор схемы компонента не передавал `exposedApi` инспектору, поэтому переключатель «Наружу» не отображался вообще. Решение: все разъёмы единственной карточки компонента едут на сцену через общий `blockPorts()` в shared (явные первыми, лимит 24); `exposedApi` возвращён со сторожевым тестом; тост об ошибке валидации теперь называет конкретное поле. Выкатка не через CI (реестр пустой, docker-логина на сервере нет) — локальная сборка `protochat-api/web:v2.5.1` + `docker save | ssh docker load`, затем `deploy.sh v2.5.1`; smoke 13/13. Фаза 13 закрыта, следующая — фаза 14 (ассистент для чужих проектов).
- Открытый хвост ProtoChat: «Ошибка валидации» при сохранении схемы проекта — причина не найдена до релиза, ждём путь поля из нового тоста в следующей сессии.
- Zettel (09:22): «Не заводить корову ради стакана молока» — принцип соразмерности решения задаче, anti-overengineering. Контекст: карусель для дочери от F130 (стакан молока) до NEMA 17 + драйвер + 12В БП (корова). Добавлена в Zettelkasten/Permanent.
- Коммитов в plasmaresis-soft и plasmaresis-firmware нет.

## 2026-09-23

- Коммитов в plasmaresis-soft и plasmaresis-firmware нет.
- Memory vault-tidy (22.09, 23:06): зафиксированы два пропущенных крючка в `1 Projects/index.md` — заметка «Пульт Higole F3APL / установка LinuxCNC 2026-09-18» и «Дипсерч-ревизия маркетинга ProtoChat 2026-09-10» были в волте, но не проиндексированы.
- Открытый хвост: PDF MKS SERVO42D/57D User Manual в `0 Inbox/` с 07.09 — ждёт переноса в проект и переименования из UUID.

## 2026-09-22

- Plasmaresis CNC / пульт: вечерняя UI-сессия (09-21 20:16 – 09-22 00:01) — иконки станка и веника реализованы из SVG; статус станка заменён на иконку в правом краю полосы состояния; настройки: выделение выбранного поля восстановлено, поле ввода над клавиатурой в рамке; список материалов выровнен под кнопкой «Материалы» без лишней подписи; кнопки страниц на всю ширину вкладки, скруглённое выделение в списках.
- Plasmaresis CNC / start-sim: добавлена подсказка остановки для Windows (`wsl -d Debian -u cnc -- bash .../start-sim.sh stop`), ранее подсказка была только для WSL.

## 2026-09-21

- Plasmaresis v2 / железо: плазматрон **HITBOX HBC65 получен** (привезли 21.09). TODO по
  подключению THC: (1) мультиметром замерить коэффициент деления на «Arc voltage output»
  в момент реза → выбрать THCAD-10 vs THCAD-300; (2) сверить разъёмы задней панели
  с шильдиком «CNC interface operation guide»; (3) прозвонить сухой контакт arc-ok.

## 2026-09-19

- Активности не было.

## 2026-09-18

- Plasmaresis / пульт: фаза 7 закрыта кодом и тестами (сетка отступов); метрика пульта
  собрана в один словарь — блок TOKENS в `plasmabot_theme.qss`, `metrics()` раздаёт
  те же числа в Python, размеры `.ui` сверяются с ним тестом; нарушений сетки в 10 видах:
  0 (против 26 на старте); гейт: 658 юнитов + 67 смоуков; инструменты:
  `tests/spacing_audit.py`, `tests/test_metrics.py`, `scripts/grid-report.py`.
- Plasmaresis / пульт / Qt-грабли: `QStackedLayout` и `QMainWindowLayout` свои поля
  не применяют (145 «призраков» из 161 в первом отчёте); рамка по box-model Qt ложится
  снаружи высоты — отсюда токены `@row-b1`/`@row-b2`; всё с замерами в
  `docs/qt-spacing-traps.md`.
- Plasmaresis / пульт / инцидент: перед работой вернул из git
  `configs/plasmabot-sim/plasmabot-sim.ini` — MOUNT был изменён top→bottom через
  страницу настроек вечером 17.09, из-за чего 31 тест был красным.
- Plasmaresis v2 / железо: выбран боевой пульт — мини-ПК **Higole F3APL** (14 493 ₽,
  Ozon); J3455, 4 ГБ, 8" тач, 1 Гбит RJ45 — критичный порт под MESA 7I98 по Ethernet;
  ОС: LinuxCNC 2.9 Live ISO (Debian 12, PREEMPT_RT — не RTAI, RTAI нужен только под LPT);
  задокументирован пошаговый план установки и latency-test в memory-vault.

## 2026-09-16

- Plasmaresis v2 / железо: клон MESA F7I98 (Spartan6 XC6SLX9, адрес 10.10.10.10) поднят
  под mesaflash 3.4.9 из WSL (`--device ETHER`); flash забэкаплена, голая GPIO-прошивка
  без stepgen.
- Plasmaresis v2 / железо: мотор X (BOB → MKS SERVO57D → NEMA23) впервые покрутился
  программными UDP-шагами без перепрошивки; граблька — SERVO57D по умолчанию En = L,
  EN надо держать LOW; 3.3 В push-pull от BOB хватило.
- Plasmaresis v2 / документация: добавлен `f7i98_x_spin.py` + runbook подключения,
  справочник MESA 7I98 и запись в BOB 5 осей ST-V2 в memory-vault.
- Грабли / сеть: второй IP на Ethernet-адаптере ПК сбросил DHCP → пропал интернет и
  Telegram (VPN Bebra не переподключился); правильный способ — временный on-link маршрут
  /32 на адрес платы (ActiveStore, DHCP не трогает).

## 2026-09-15

- Активности не было.

## 2026-09-11

- Plasmaresis DB / откат фазы 10: `main` в `plasmaresis-db` сброшен на v2.0.2,
  план и папка `phase-10-ports-and-wires` удалены, теги v2.2.0–v2.2.2 убраны
  локально и на GitLab; прод откачен образами v2.0.2 (smoke 13/13, данные
  пережили откат — лишние поля `detail`/`labels` Zod молча отбрасывает).
- Plasmaresis DB / релиз v2.0.3: планка клемм (`strip`-узел без шапки),
  поворот картинки в карточке (90/180/270), свой цвет линии связи, окно
  разъёмов с drag&drop строк; схема v1-klipper перенесена в прод через API
  (9 узлов / 20 связей, smoke 13/13); гейт был красным из-за тестовых
  фикстур — `color` у связи не было в `shared`, поправлено до деплоя.
- Plasmaresis DB / GitLab: расхождение `main` закрыто слиянием `-s ours` —
  дерево от v2.0.3, история remote остаётся в предках, force-push не нужен;
  тег v2.0.3 запушен.
- Plasmaresis v2 / железо: задокументирована фазовая разводка HANPOSE 23HS5628
  (жгут плеча, проверено 2026-09-10); даташит подтверждает паспортные
  параметры, но цвета жил жгута расходятся с фактической разводкой.

## 2026-09-10

- Plasmaresis / пульт: заведён новый репозиторий `plasmaresis-cnc` — планшетный
  пульт Plasmabot как обёртка над QtPlasmaC (LinuxCNC + MESA 7I98 + 5-осевая BOB +
  MKS SERVO57D + Hanpose 23HS5628). Репозиторий пока без remote.
- Plasmaresis / архитектура пульта: разведка живой установки LinuxCNC 2.9.4 дала
  ключевые выводы — плазма-логика целиком в HAL-компоненте `plasmac` (~177 пинов),
  экран лишь лицо к нему; официальные варианты экрана (4×3, 9×16) = свой `.ui` +
  симлинк на общий handler; handler ожидает 268 виджетов по objectName → наследовать
  нельзя, берём донором кода (лог в `docs/borrowed.md`, лицензия GPLv2); qtvcp ищет
  экран в `$CONFIG_DIR` — системные файлы LinuxCNC не трогаем никогда.
- Plasmaresis / план: 6 фаз — фазы 1–4 на симуляторе (без железа), фазы 5–6 после
  приезда плат. Фаза 1 расписана по трём волнам: симконфиг → скелет экрана → гейт
  + тесты. Детали в `plan/plan-info.md` и `plan/phase-1-skeleton-sim/`.
- Plasmaresis / стек: Python/PyQt5 (qtvcp), C для кинематики (`arm2rkins`); `halcompile`
  и хедеры в системе отсутствуют — под кастомную кинематику понадобится пакет
  `linuxcnc-uspace-dev`.

## 2026-09-09

- Нет коммитов во всех репозиториях за сутки. Текущий срез без изменений:
  Plasmaresis DB — фазы 2 и 5 завершены (Telegram-мост, веб-чат, Vue Flow
  канвас схемы проекта); Plasmaresis v2 — принято решение LinuxCNC + MESA
  7I98 + SERVO57D step/dir, распиновка BOB → SERVO57D задокументирована.

## 2026-09-08

- Нет коммитов во всех репозиториях за сутки. Текущий срез: Plasmaresis DB —
  фазы 2 и 5 завершены (Telegram-мост, веб-чат, Vue Flow канвас схемы
  проекта); Plasmaresis v2 — принято решение LinuxCNC + MESA 7I98 +
  SERVO57D step/dir, распиновка BOB → SERVO57D задокументирована.

## 2026-09-07

- Plasmaresis DB / фаза 2: завершена — токен Telegram-бота, pdb и skill
  развёрнуты на xorek, страница Черновики, веб-чат, интеграционные тесты.
- Plasmaresis DB / фаза 5: завершена — канвас схемы проекта на Vue Flow
  (железо со связями, версия, редактор, бот) добавлен в заметку и крючок
  в `1 Projects/index.md`.
- Plasmaresis v2 / железо: принято решение — остаёмся на LinuxCNC + MESA
  7I98 + QtPlasmaC; MKS SERVO57D V1.2 ставим в режим STEP/DIR (CAN-ветка
  отложена). Задокументирована распиновка BOB → SERVO57D (common-anode:
  V+ → Com, CLK → Stp, CW → Dir, EN → En). Из BOM отбракованы кабель
  DB25 папа-папа и CNC Shield V3 — не подходят для этой ветки.

## 2026-09-06

- Документация / железо: в 3 Resources/Hardware (LFS) добавлены мануалы и
  заметки-справочники четырёх компонентов — Manta M4P (скилл `manta-m4p-expert`),
  MKS SERVO57D V1.2 мануалы CAN+RS485 V1.0.9 (питание только 12–24 В, скилл
  `mks-servo57d-expert`), BOB 5 осей ST-V2/BL-MACH-V1.1 (карта DB25→клеммы,
  перемычки Relay/PW(5V), грабли под 7I98, скилл `bob-5axis-expert`), MESA 7I98
  мануал V1.0 (перемычки, IP, распиновка HDR26-DB25, mesaflash, 5ABOBx3).
- Plasmaresis / закупка v2: зафиксирован кабельный маршрут 7I98 → шлейф →
  DB25 папа-папа → BOB → CNC Shield; создан репозиторий Plasmaresis DB.
- Plasmaresis DB / фаза 1: закрыта — монорепо, API, аутентификация,
  редактирование, документация.
- Plasmaresis DB / фаза 2: спроектирована — мост с Telegram-ботом, 6 волн
  реализации зафиксированы в заметке v2-закупка/Plasmaresis DB.

## 2026-09-04

- Нет коммитов во всех репозиториях за сутки. Текущий срез без изменений: CB1
  настроен (алю-плечи 150/150, gear ratio 1:3, шаг CL57 2.5 мкс), мотор-тесты
  завершены (потолок 40 рад/с, лимит — вход драйвера 500 кГц), move_guard +
  reach_check задеплоены на CB1 (reach_check выключен под макет), THC-тракт v1
  зафиксирован (HBC65 → THCAD → 7I98), QtPlasmaC развёрнут в WSL2.

## 2026-09-02

- Нет коммитов во всех репозиториях за сутки. Текущий срез без изменений: CB1
  настроен (алю-плечи 150/150, gear ratio 1:3, шаг CL57 2.5 мкс), мотор-тесты
  завершены (потолок 40 рад/с, лимит — вход драйвера 500 кГц), move_guard +
  reach_check задеплоены на CB1 (reach_check выключен под макет), THC-тракт v1
  зафиксирован (HBC65 → THCAD → 7I98), QtPlasmaC развёрнут в WSL2.

## 2026-08-31

- Нет коммитов во всех репозиториях за сутки. Текущий срез: CB1 настроен (алю-плечи
  150/150, gear ratio 1:3, шаг CL57 2.5 мкс), мотор-тесты завершены (потолок 40 рад/с,
  ограничение — вход драйвера 500 кГц), THC-тракт v1 зафиксирован (HBC65 → THCAD →
  7I98), тестовая среда QtPlasmaC развёрнута в WSL2.

## 2026-08-30

- Plasmaresis / железо: зафиксирована архитектура THC-тракта v1. Источник —
  HITBOX HBC65 (CNC-ready, arc voltage output на задней панели). Мозг движения —
  MESA 7I98 + 5-осевой BOB + THCAD. Baseline v1: фиксированная скорость из cut
  chart + THC по напряжению (только ось Z). Adaptive-feed-по-напряжению отложен
  как послетиховой эксперимент (хук `motion.adaptive-feed` + HAL-компонент).
  THCAD подключается напрямую на encoder-вход 7I98, минуя BOB (PC817 ~1 МГц не
  пропускает). Открытый вопрос: коэффициент деления arc voltage output на HBC65
  (1:50 → THCAD-10; сырой → THCAD-300) — нужен крупный кадр шильдика.
- Plasmaresis / memory: добавлен крючок Leadshine EL7 в `1 Projects/index.md`.
- C-3PO / инфраструктура: инцидент 29.08 — токены Claude CLI обнулились без
  ребута (аптайм 30 дней, обнуление 28.08 13:38). Добавлена секция «Известные
  отказы» с диагностикой: `claude auth status` + `openclaw channels status`
  разделяют обнулённые токены (#1) и залипший Telegram-поллинг (#2).
- Рабочая машина: MSI Katana GF66 12UE — разблокировка BIOS
  (LAlt+RCtrl+RShift+F2), путь андервольта (i7-12650H, Processor/Ring offset,
  CEP Disable, андервольт загрузился стабильно). Корень перегрева — режим
  Смарт-авто давил кулеры до 3600 rpm; фикс: Extreme Performance. Под 100%
  нагрузкой ~95°C — ограничение корпуса, лечится PL1-cap, не обдувом.

## 2026-08-26

- Прошивка / sim: два комплекта gcode-тестов для обкатки SCARA-руки на макете.
  `spin-demo.gcode` — FORCE_MOVE (мимо кинематики Klipper), каждый мотор отдельно,
  3 цикла по 2 оборота туда/обратно, 10 рад/с (~95 rpm); пригоден до
  SET_KINEMATIC_POSITION. `spin-both.gcode` (+ генератор `gen_spin_both.py`) —
  скоординированный ход через IK: тулхед описывает окружность r=100 вокруг базы,
  144 сегмента на круг, 10 кругов CCW + 10 CW, оба сустава одновременно, F60000.
- Прошивка / sim: харнес для воспроизведения краша 2026-08-03 (телеоп после мастера
  позиции). `repro-2026-08-03-wizard.gcode` — минимальный сценарий: SET\_KINEMATIC\_POSITION
  X=-330 Y=-470, пауза 9 с, G1 до рабочей точки (F3000). `repro_crash_wizard.py` —
  Python-обёртка над repro\_c\_harness с геометрией CB1 (L1=L2=300, elbow=down);
  прогоняет сценарий для трёх осей (shoulder / forearm / cartesian-X) и трёх
  timing-окон (0.5 / 0.1 / 0.05 мс) — итого 9 комбинаций.

## 2026-08-25

- Развёрнута тестовая среда QtPlasmaC: LinuxCNC 2.9.4 из репозитория Debian
  trixie в WSL2, GUI отдаётся в браузер через VNC (WSLg на этой машине создаёт
  окна, но не рисует их). Заметка добавлена в Plasmaresis/память.

## 2026-08-24

- Ночной разбор архитектуры v2: CAN/CANopen vs хоббийный CAN, сравнительная
  таблица интегрированных приводов NEMA23 (Josh CANBUS / MKS SERVO57D / JSS
  ESS57C / UIROBOT UIM5756CA / Applied Motion TSM23). Важное уточнение: LinuxCNC
  не был отклонён — старая запись в Plasmaresis-Info исправлена; для v2 переоткрыт
  как кандидат в CANopen-мастер (LinuxCNC + QtPlasmaC = THC из коробки). Добавлена
  заметка «v2 — выбор архитектуры приводов» с двумя ветками: B (CANopen +
  ESS57C 3Н·м 48В + LinuxCNC/QtPlasmaC) и A (MKS SERVO57D + свой мастер + свой фронт).
- Пересмотр позиционирования v2: моат продукта — рука-юнит, готовый QtPlasmaC
  считается плюсом, а не потерей своего UI.
- Добавлена ветка C для v2: LinuxCNC + MESA + step/dir + THCAD (гибрид без
  полного CANopen). Влит путь закупки: 7I98 + внешний BOB, шлейф DB25↔FC-26
  (221 ₽); USB-LPT — не нужен/не работает.
- Docs: починен скрипт daily-log.ps1 — промпт передаётся через stdin
  (многострочный аргумент ломал argv), run-лог добавлен в .gitignore.

## 2026-08-23

- Настройка руки закончена - найдена причина двухдневной путаницы (вал J1);
  выставлены знаки углов под слитные ходы, зафиксирована методика проверки.
- Опубликован этот сайт документации (VuePress + GitHub Pages): разделы
  "Станок и подключение", "Редактор", "Рез", "CAM-ядро". Главы про рез -
  черновики до завершения волны 4 фазы 25.
- Идея v2 - самодостаточный контроллер (ESP32-S3 + планшет, форк FluidNC) -
  зафиксирована и отложена: сначала доделать текущую версию. Попутно разобраны
  мозги ArcDroid (SKR 1.4 Turbo + модифицированный Marlin, MORGAN_SCARA).
- Сайт документации: добавлена вкладка "Лог", настроена ежедневная автозапись
  в 12:00 (Task Scheduler вызывает claude -p, затем deploy.ps1).

## 2026-08-20

- Прошивка: обновлён printer-cb1.cfg под алюминиевые плечи 150/150 и ремни 1:3
  (gear_ratio 3:1, DIP 40000 остаётся); пины моторов поменяны местами, dir
  инвертированы под перекинутую проводку; скорости на обкатку 300/1500.
