Ты ведёшь ежедневный лог проекта Плазмобот на сайте документации
(текущий репозиторий d:\projects\plasmabot-docs). Сделай строго следующее и
ничего сверх этого.

1. Собери события за последние 26 часов (git log --since="26 hours ago",
   --all не нужен):
   - d:\projects\plasmaresis-soft (софт станка)
   - d:\projects\plasmaresis-firmware (прошивка руки)
   - d:\projects\plasmabot-docs (этот сайт)
   - d:\projects\memory - только коммиты, касающиеся Plasmaresis/Плазмобота;
     при необходимости прочитай изменённые заметки в "1 Projects/Plasmaresis/".

2. Отбери важное: завершённые волны/фазы, принятые решения, настройка и
   изменения железа, найденные и решённые проблемы, публикации. Пропускай
   мелочь: merge-коммиты, правки опечаток, служебные deploy-коммиты и
   вчерашние события, уже записанные в лог.

3. Если важного не было - заверши работу, НИЧЕГО не меняя. Пустые записи
   ("ничего не произошло") не нужны.

4. Иначе открой docs/log/README.md и вставь запись сразу после строки
   <!-- LOG:INSERT -->:

   ## ГГГГ-ММ-ДД

   - пункт: кратко, по делу, в прошедшем времени

   Если запись с сегодняшней датой уже существует - дополни её новыми
   пунктами, не дублируя имеющиеся. Типографика: только дефис "-", никаких
   тире; кавычки только прямые "".

5. Закоммить и запушь исходники:
   git add docs/log/README.md
   git commit -m "Лог: ГГГГ-ММ-ДД"
   git push

6. Задеплой сайт: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/deploy.ps1
   Убедись по выводу, что деплой прошёл ("deploy ok").
